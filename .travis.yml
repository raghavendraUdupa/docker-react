language: generic
sudo: required
services:
- docker

before_install:
 - docker build -t ragpark/docker-react -f Dockerfile.dev .
sscript:
 - docker run ragpark/docker-react npm run test -- --coverage
